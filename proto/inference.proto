syntax = "proto3";
package inference;

service InferenceService {
  // 简单起步：Unary RPC。后续可以改成 streaming
  rpc Generate (GenerateRequest) returns (GenerateResponse);
}

message GenerateRequest {
  string prompt = 1;
  int32 max_tokens = 2;
}

message GenerateResponse {
  string text = 1;
}
